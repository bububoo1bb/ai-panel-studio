// Shared types and utilities for AI Panel Studio
// This package will contain types shared between frontend and backend
// (e.g., Discussion, Expert, Message interfaces)

export const APP_NAME = "AI Panel Studio";

export interface HealthResponse {
  status: string;
}
